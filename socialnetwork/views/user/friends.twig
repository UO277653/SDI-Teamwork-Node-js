{% extends "layout.twig" %}
{% block title %} Friends – Social Network {% endblock %}
{% block main_container %}
    <h2>Friends</h2>
    <div class="table-responsive">
        <table class="table table-hover" id="tableFriends">
            <thead>
            <tr>
                <th scope="col">Email</th>
                <th scope="col">Name</th>
                <th scope="col">Surname</th>
                <th scope="col">Publications</th>
            </tr>
            </thead>
            <tbody>
            {% for friend in friends %}
                <tr>
                    <td>{{ friend.email }}</td>
                    <td>{{ friend.name }}</td>
                    <td>{{ friend.surname }}</td>
                    <td><a href="/publications/list/{{ friend.email }}">List of publications</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <!-- Paginación mostrar la actual y 2 anteriores y dos siguientes -->
    <div class="row text-center">
        <ul class="pagination">
            {% for page in pages %}
                <li class="page-item" id="id-{{ page }}">
                    <a class="page-link" href="/friends/list/?page={{ page }}">{{ page }}</a>
                </li>
            {% endfor %}
            <script>
                $("#pi-"+"{{ currentPage }}").addClass("active");
            </script>
        </ul>
    </div>
{% endblock %}